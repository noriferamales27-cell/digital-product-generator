# Required in production. Get one at https://console.anthropic.com
ANTHROPIC_API_KEY=

# Optional. Set to 1 to run the UI with canned data and no API calls.
MOCK_MODE=

# Optional. Simple shared password so only you can run the generator.
# Leave empty to run it open (fine for local dev, not for a public deploy).
APP_PASSWORD=
